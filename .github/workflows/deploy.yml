name: Deploy to VPS

on:
  push:
    branches: ["master"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Start Tailscale
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ secrets.TAILSCALE_AUTH_KEY }}
          args: --ssh=false

      - name: Set up SSH key
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          head -n 1 ~/.ssh/id_ed25519 | grep -q "BEGIN OPENSSH PRIVATE KEY" || (echo "SSH_PRIVATE_KEY is not a private key" && exit 1)
          ssh-keygen -l -f ~/.ssh/id_ed25519 >/dev/null
          ssh-keyscan 100.121.244.99 >> ~/.ssh/known_hosts

      - name: Deploy on VPS
        run: |
          ssh -i ~/.ssh/id_ed25519 -o IdentitiesOnly=yes didi@100.121.244.99 << 'EOF'
            set -e
            cd /home/didi/Desktop/template-vps
            git pull origin master
            docker compose pull
            docker compose up -d
          EOF
